spring:
  jpa:
    properties:
      hibernate:
        ddl-auto: validate
        dialect: org.hibernate.dialect.PostgreSQL95Dialect
  datasource:
    url: jdbc:postgresql://localhost:5432/DiplomaWork
    username: postgres
    password: 2202

security:
  token:
    secret: sdf548@!#df4884d5g4r68dfg1r#@#@$sd84e61f63f@##s6d5f#@f@#f#@@5f45s6f3@
    client-ip: Client ip
    issuer: Astanait.edu.kz server
    token-cannot-be-verified: Token can not be verified
    token-prefix: 'Bearer '
    audience: Astanait.edu.kz client
    expiration-time: 360_000_0
    jwt-token-header: Jwt-Token


email:
  verification:
   simple_mail_transfer_protocol: smtp
   username: test.diploma@outlook.com
   password: DiplomaWork2022
   from_email: noreply@astanait.edu.kz
   email_subject: Verification account
   gmail_smtp_server: smtp.office365.com
   smtp_host: smtp.office365.com
   smtp_auth: mail.smtp.auth
   smtp_port: mail.smtp.port
   default_port: 587
   smtp_starttls_enable: mail.smtp.starttls.enable
   smtp_starttls_required: mail.smtp.starttls.required